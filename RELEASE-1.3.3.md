# 轻阅 1.3.3 / 轻阅 Lite 0.1.1

## 本次更新

1. 修复未运行时双击 Markdown，打开后却停留在历史首个标签的问题。启动文件队列与会话恢复按顺序完成，双击文件最后激活；启动期间收到的新文件也不会遗漏。
2. 设置中新增默认 Markdown 布局：阅读、编辑、分栏。新建空白文档直接进入编辑，非 Markdown 文本使用编辑模式。
3. 设置中新增无文档启动、空白 Markdown 启动、恢复上次标签。前两种模式从外部打开文件时，仅打开指定文件，不带出历史标签。
4. 全新启动前保留上次标签恢复快照；空白页和设置中提供“恢复上次标签”。干净标签的阅读位置也会在退出时保存，保存会话请求按队列处理，避免自动保存与退出保存抢写。
5. 设置中增加“记住上次文件夹”；关闭后下次不自动加载文件夹，另有“清空文件夹”即时重置。此设置与是否恢复文档相互独立。
6. 目录与文件夹面板预留真实宽度，在编辑、分栏、阅读模式中推开正文，不再悬浮遮挡；同时打开时各自保留滚动区域。目录仍同步定位编辑行与预览标题。
7. 修复 Mermaid 的 HTML/SVG 混合标签被净化器误删导致节点空白的问题，继续过滤脚本，并为明亮、深色、护眼、纸张主题配置文字颜色。
8. 注册文件关联时补充 Windows 桌面右键“新建 Markdown 文档”。已有其他软件的新建模板保留，不强行覆盖。已存在的 MD 默认应用不被抢占；没有类型注册时才补齐默认类型。卸载关联只移除属于本版的新建项。
9. 移除顶栏重复“+”，保留标签栏的新建按钮；空白页提供带文字的新建入口。深色工具图标、目录标题与滚动条同步修整。
10. 格式化操作补充结果提示，并支持 YAML 排版。JSON 整理缩进、换行；JSONC、JS/TS、CSS、HTML 使用内置格式化器。Markdown 格式化按钮禁用，预览本身不依赖该按钮。

## 轻量版边界

移除 Word/Excel、白板、思维导图编辑、独立图片编辑、文件对比、结构校验、格式化模块，并换用精简文本编辑器。不会加载完整编辑器和语言工作线程。Markdown 的图片/音频附件显示和插入仍保留；这与独立图片编辑工具不同。

保留文本编辑/查找/撤销、多标签、恢复历史、文件夹搜索及备份后批量替换、Markdown 渲染、Mermaid、公式、书签批注、主题、朗读与阅读缩放。代码文件以普通文本编辑，不承诺 IDE 级语法服务。单文件最大 32 MB；超过 8 MB 切换无预览的大文件模式。恢复快照有大小上限，大文件编辑务必主动保存。

完整版与轻量版的数据、注册标识独立，可以同时保留。轻量版并非零运行时：它仍随包附带 Electron，因此磁盘体积不会降到几 MB。推荐 ZIP 极速包，解压一次后直接启动；单 EXE 便携包每次启动要释放运行文件，耗时更多。

## 三个问题的说明

### 格式化文档有什么用？

它改变的是源文件排版，例如将 `{"a":1,"b":2}` 整理成分行、统一缩进的 JSON，不负责“编译” Markdown，也不修复业务数据。内容已规范时没有肉眼变化；不支持的语言、大文件模式或语法错误也可能无法格式化。现在会明确提示是否有变化及失败原因。

### 文件夹要不要保留？

现在可以选择。到设置勾选“记住上次文件夹”并保存；不勾选时下次打开是空的，当前仍可继续使用已经选中的文件夹。点击“清空文件夹”立即重置，不会删除磁盘文件。

### 无服务器便携版如何按需加载/卸载？

可以将功能代码分成随包携带的本地模块，点击相应功能时通过动态导入加载；无须服务器，也不会上传文档。本版 Mermaid 等组件已经按需加载。Lite 不包含被移除的重型功能，不能仅靠开关恢复它们。

“按需加载”不等于“完全卸载内存”：关闭组件能释放它的界面、监听器与模型，但已导入的 JavaScript 通常仍有缓存。若以后做可装卸的离线扩展包，可用独立 Worker/进程隔离，关闭时销毁进程；同时需要版本兼容检查和包完整性验证。这次没有加入自动下载或任意外部插件执行器。

## 使用与迁移

- 建议把极速 ZIP 解压到固定目录，运行其中的 EXE；不要单独搬走 EXE。
- 1.3.3 默认“无文档启动、Markdown 分栏、不记住文件夹”。在设置里改好后点“保存启动设置”。
- 要保留完整版旧设置：先退出旧版，再复制旧程序旁的整个 `QingYue-Data` 到新版程序旁；保留原目录作为备份。不要将其覆盖到轻量版数据目录。
- 在新版设置中注册文件关联，Windows 指向新版位置。MD 若仍由旧应用默认打开，用系统“打开方式”选择新版并设为默认。
- BAT、CMD、PowerShell 等可执行脚本继续不作为轻阅的系统关联类型，仍可以从应用内部打开编辑。
- 新建菜单由 Windows Explorer 缓存；注册后必要时刷新桌面，Windows 11 中可检查“显示更多选项”。注册表模板测试不等同于所有 Windows 版本的 Explorer 实测。

## 验证

TypeScript 检查与 Node 自动化测试；实际 Electron UI 覆盖启动文件优先级、延迟 Office 恢复、设置保存、空白/无文档/历史模式、两种窗口宽度的三种布局、目录跳转、四种主题图表和两版文本保存。测试在隔离数据目录运行，不修改用户现有注册表或文档。
